<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity WardCover.
    -->
    <changeSet id="20260925124305-1" author="jhipster">
        <createTable tableName="ward_cover" remarks="Which doctor is responsible for which ward, for a period. Without this the\n&quot;a doctor sees admissions on a ward they are covering&quot; rule has no data to read.">
            <column name="id" type="bigint">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="covers_from" type="${datetimeType}">
                <constraints nullable="false" />
            </column>
            <column name="covers_to" type="${datetimeType}">
                <constraints nullable="true" />
            </column>
            <column name="note" type="varchar(500)">
                <constraints nullable="true" />
            </column>
            <column name="doctor_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <column name="ward_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <column name="assigned_by_id" type="bigint">
                <constraints nullable="false" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
        <dropDefaultValue tableName="ward_cover" columnName="covers_from" columnDataType="${datetimeType}"/>
        <dropDefaultValue tableName="ward_cover" columnName="covers_to" columnDataType="${datetimeType}"/>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20260925124305-1-data" author="jhipster" context="faker">
        <loadData
                  file="../fake-data/ward_cover.csv"
                  relativeToChangelogFile="true"
                  separator=";"
                  tableName="ward_cover"
                  usePreparedStatements="true">
            <column name="id" type="numeric"/>
            <column name="covers_from" type="date"/>
            <column name="covers_to" type="date"/>
            <column name="note" type="string"/>
            <column name="doctor_id" type="numeric"/>
            <column name="ward_id" type="numeric"/>
            <column name="assigned_by_id" type="numeric"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
